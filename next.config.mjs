/** @type {import('next').NextConfig} */
const nextConfig = {
  // Amplify Gen 2 outputs are loaded at runtime; no special webpack config needed.
  reactStrictMode: true,
};

export default nextConfig;
