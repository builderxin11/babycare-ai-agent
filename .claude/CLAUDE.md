# Local Claude Code Instructions (NOT committed to git)

## Git Commit Rules (MANDATORY)
- **Every** `git commit` MUST use `--author="builder-xin <builderxin11@gmail.com>"`.
- **Never** use the system default git author. This is non-negotiable.
